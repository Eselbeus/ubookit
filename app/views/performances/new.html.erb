<!-- <% user = current_user%> -->
<%= form_for Performance.new do |f|%>
  <%= f.hidden_field :musician_id,:value => current_user.id%> <br>
  <%= f.collection_select :venue_id, Venue.all, :id, :name%> <br>
  <%= f.datetime_field :time %> <br>
  <%= f.submit %>
  <%end %>
  